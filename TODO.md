# TODO

- [ ] Add a `subscribe` route and a broker-backed integration test gated behind
      an `--online` flag with a containerised broker.
- [ ] Add retained-message and QoS-2 coverage.
- [ ] Add a Docker smoke matching the other `urirun-connector-*` repos.
- [ ] Promote the hub catalog entry from `planned` to `available` once published.
